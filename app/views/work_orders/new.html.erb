<div class="max-w-3xl mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to t(".back"), work_orders_path, class: "text-sm text-zinc-500 hover:text-zinc-700" %>
    <h1 class="text-2xl font-semibold text-zinc-900 mt-2"><%= t(".title") %></h1>
    <p class="text-sm text-zinc-500 mt-1"><%= t(".subtitle") %></p>
  </div>

  <%= render "form", work_order: @work_order %>
</div>
